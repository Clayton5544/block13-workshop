
const convertToCelsius = (fahrenheit) => {
    return (fahrenheit) -32 * .5555556;
    prompt(`Enter a number and I will convert it to celsius`);
}
  //prompt(`Enter a number and I will convert it to celsius`);

  console.log(fahrenheit(90));



  const createMessage = (fahren, celc) => {

  }

  const rand = (limit) => {

  }